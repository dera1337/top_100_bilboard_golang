1. bkin file main.go
2. terminal go mod init
3. bkin file go.sum
4. terminal git init
5. bkin branch baru git checkout -b main
6. git add . Or manual dari tab kiri github
7. git commit -m "WIP project 100"
8. git remote add origin "https://github.com/dera1337/top_100_bilboard_golang.git"
9. git push origin main

5. bkin branch baru git checkout -b implement-web-scraper
6. git add . Or manual dari tab kiri github
7. git commit -m "WIP project 100"
8. 
9. git push origin implement-web-scraper

-git branch -D implement-web-scraper

-buat sinkron cek git branch -r

-trus cara singkron git remote prune origin

func ScrapeYoutube(url string) string {

// 	res, err := http.Get(url)

// 	if err != nil {
// 		log.Fatal(err)
// 	}
// 	defer res.Body.Close()
// 	if res.StatusCode != 200 {
// 		log.Fatalf("status code error: %d %s", res.StatusCode, res.Status)
// 	}

// 	doc, err := goquery.NewDocumentFromReader(res.Body)
// 	if err != nil {
// 		log.Fatal(err)
// 	}

// 	youtube, _ := doc.Find("#search > div > ol > li:first-child > :first-child").Attr("data-rurl")
// 	// #left > div div >h3 > a "href"
// 	// #search > div > ol > li:first-child > div:first-child "data-rurl"
// 	fmt.Println(youtube)

// 	return youtube

// }
